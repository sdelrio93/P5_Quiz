<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
<h1>
	Preguntas:
</h1>
<table>
	<% for (var i in quizzes) { %>
	<% var quiz = quizzes[i]; %>
	<tr>
		<td>
			<a href="/quizzes/<%= quiz.id %>/play"><%= quiz.question %></a>
		</td>
		<td>
			<a href="/quizzes/<%= quiz.id %>"><button>Ver</button></a>
		</td>
		<td>
			<a href="/quizzes/<%= quiz.id %>/edit"><button>Editar</button></a>
		</td>
		<td>
			<a href="/quizzes/<%= quiz.id %>?_method=DELETE"
			onClick="return confirm("Delete: <%= quiz.question %>");">
			<button>Borrar</button>
			</a>
		</td>
		</tr>
	<% } %>
</table>
<a href="/quizzes/new"><button type="button">Crea una nueva pregunta</button></a>
</body>
</html>